node_modules/
.vercel/
.env*
.DS_Store
Thumbs.db

# Generated by build.js for deployment
public/books/
public/lesson-plans/
public/books-manifest.json

# His progress + the news cache stay on the machine that made them.
# (Live progress lives in the database; use Parent Zone -> Export to move it.)
data/state.json
data/news-cache.json

.vercel
.env*

# Agent docs installed by the Upstash marketplace integration.
# Machine-specific (the .claude/skills entries are absolute symlinks) and
# nothing to do with the app itself.
.agents/
.claude/skills/
skills-lock.json
